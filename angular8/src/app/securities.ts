export interface Securities {
    accessToken: string
    expiresAt: string
    state: boolean
    userIdentifier: string
    userType: string
} 

export interface Support{
    code: number
    message: string
    time: string
}

export interface Logouts{
    tokenn: string
}
